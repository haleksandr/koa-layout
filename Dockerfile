FROM node:13

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . /app

ARG PORT
ARG HOST_DB
ARG DB_DB
ARG PASSWORD_DB
ARG USERNAME_DB

ENV PORT=$PORT
ENV HOST_DB=$HOST_DB
ENV DB_DB=$DB_DB
ENV PASSWORD_DB=$PASSWORD_DB
ENV USERNAME_DB=$USERNAME_DB

EXPOSE $PORT

CMD ["npm", "serve"]